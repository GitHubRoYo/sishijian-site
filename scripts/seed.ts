#!/usr/bin/env tsx
/**
 * Seed Script (Strapi)
 *
 * TODO: Implement seeding via Strapi REST API.
 * The previous Payload CMS seed script has been removed.
 *
 * To seed Strapi, use one of:
 *   1. Strapi Admin Panel (http://localhost:1337/admin)
 *   2. Strapi REST API: POST /api/{collection} with JSON body
 *   3. Strapi CLI: strapi import / strapi transfer
 *
 * Usage: pnpm seed
 */

console.log('⚠️  Seed script not yet implemented for Strapi.')
console.log('   Use the Strapi Admin Panel at http://localhost:1337/admin to add content.')
process.exit(0)
