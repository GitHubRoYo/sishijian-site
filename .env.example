# Database (required)
DATABASE_URI=postgresql://sishijian:sishijian123@localhost:5432/sishijian

# Payload CMS (required)
PAYLOAD_SECRET=your-secret-key-here-change-in-production
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# Next.js (required)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LOCALE=zh-HK
NEXT_PUBLIC_LOCALES=zh-HK,en,zh-Hans

# Admin Credentials (used by seed script)
ADMIN_EMAIL=admin@sishijian.com
ADMIN_PASSWORD=admin123

# SMTP (optional, for contact forms)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@sishijian.com.hk

# Vercel Blob (optional, recommended in production for Media uploads)
BLOB_READ_WRITE_TOKEN=

# Remote seed (optional, call /seed?token=... once after deploy)
SEED_SECRET=
